// import * as React from 'react';
// import { connect } from 'react-redux';
// import TableProps from '../types/TableProps';
// import TypeState from '../types/TypeState';


// const PriceList = (props: TableProps) => {
//     ({price: props.price})
// };

// const mapStateToProps = (state: TypeState) => {
//     return {
//         price: state.price
//     };
// };

// export default connect<TypeState, null>(mapStateToProps)(PriceList);